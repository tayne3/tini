# Global keys (no section header)
# These keys appear before any [section]

global_key1 = global_value1
global_key2 = global_value2
app_name = GlobalApp
debug = true

# Now a regular section
[section1]
key = value
