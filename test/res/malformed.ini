# Malformed INI Test File
# Intentionally broken for error handling tests

# Missing section close bracket
[unclosed section
key1 = value1

# Missing equals sign
[bad_keys]
key_without_value
another_missing_value
valid_key = valid_value

# Multiple equals signs
[equals]
multi_equals = a = b = c

# Stray characters
[stray]
key = value garbage here without comment marker

# Empty section name
[]
key = value

# Just brackets
][

# Nested brackets
[[nested]]
key = value

# Section in value (literal)
[literal]
key = [not a section]
