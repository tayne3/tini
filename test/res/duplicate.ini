# Duplicate Section/Key Test File

[database]
host = first_host
port = 5432

# Same section name again - should merge
[database]
user = admin
host = second_host

[settings]
timeout = 30

# Duplicate key in same section
[user]
name = Alice
email = alice@example.com
name = Bob
role = admin
name = Charlie
